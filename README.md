# Feven' Prints

**Photo prints, albums & gifts — made locally in Addis Ababa.**

A fresh, photography-forward catalog site for a local print shop. Mobile-first, bilingual-ready, built by Tobia.

**Live:** https://messaynew-cyber.github.io/feven-prints/

## Setup notes
- The `images/` folder currently holds branded **placeholders**. Replace with Feven's real photos as she gets them.
- Product prices in `index.html` (product cards) are **ETB placeholders** — update to real pricing.
- Contact: replace `PHONE` and `TG_USER` at the bottom of the `<script>` in `index.html` with Feven's real WhatsApp number + Telegram username.
